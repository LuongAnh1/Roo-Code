# ============================================================================
# requirements.txt - Dependencies cho Roo-Code Project
# ============================================================================
# Cách dùng:
# 1. Cài đặt tất cả dependencies:
#    pip install -r requirements.txt
#
# 2. Cài đặt từng package:
#    pip install manim
#    pip install manim-voiceover
#
# 3. Cập nhật requirements khi cài package mới:
#    pip freeze > requirements.txt
# ============================================================================

# ============================================================================
# CORE DEPENDENCIES (Manim & Animation)
# ============================================================================
manim==0.18.0              # Animation library dùng để tạo video từ code
numpy>=1.21.0              # Numerical computing (dùng trong manim và xử lý ảnh)
Pillow>=9.0.0              # Image processing library (dùng cho ImageMobject)
pydub>=0.25.1              # Audio processing (yêu cầu của manim)

# ============================================================================
# VOICEOVER & TEXT-TO-SPEECH
# ============================================================================
manim-voiceover>=0.3.4     # Voiceover support cho manim scenes
gTTS>=2.2.4                # Google Text-to-Speech (dùng trong GTTSService)
googletrans>=4.0.0         # Google Translate API (dùng để dịch phụ đề)

# ============================================================================
# VIDEO & MEDIA PROCESSING
# ============================================================================
moviepy>=1.0.3             # Video editing (dùng trong manim để tạo video)
imageio>=2.14.0            # Image I/O support
imageio-ffmpeg>=0.4.7      # FFmpeg backend cho imageio

# ============================================================================
# UTILITIES
# ============================================================================
scipy>=1.7.0               # Scientific computing (dùng trong manim)

# ============================================================================
# OPTIONAL: Jupyter & Development
# ============================================================================
# jupyter>=1.0.0           # Bỏ comment nếu muốn chạy main.ipynb
# ipython>=7.0.0           # Bỏ comment cho interactive shell
